import React from 'react';
import { Link } from 'react-router-dom';
import styled from 'styled-components';

const BtnIcon = styled.button`
    border: none;
    background-color: tranparent;
`;

export const ButtonIcon = ({}) => {
    return <BtnIcon>btnicon</BtnIcon>;
};
